using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RedDude : Enemy
{
    // Start is called before the first frame update
    void Start()
    {
        _Init();
    }

    // Update is called once per frame
    void Update()
    {
        _Update();
    }
}
